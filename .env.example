# Server bind
VFX_MCP_HOST=127.0.0.1
VFX_MCP_PORT=11122

# FFmpeg binary paths (override if not on PATH)
VFX_MCP_FFMPEG_PATH=ffmpeg
VFX_MCP_FFPROBE_PATH=ffprobe

# FFmpeg subprocess timeout in seconds
VFX_MCP_TIMEOUT_SECONDS=300
