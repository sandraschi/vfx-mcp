## Session Context (vfx-mcp)

FastMCP 3.4+ FFmpeg video effects server. Portmanteau tool `vfx_apply` with 9 operations: color_grade, transition, chroma_key, overlay_text, overlay_image, blur, speed, crop, concat.
