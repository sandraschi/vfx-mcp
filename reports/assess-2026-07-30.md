# Assessment: vfx-mcp

**Date**: 2026-07-30
**Repo Type**: Standard MCP Server
**SOTA Score**: 30/100 (<60 = runt, fails fleet bar)

## Summary

vfx-mcp has a solid core (FFmpeg video effects, FastMCP 3.4+, dual transport,
CORS, health/diagnostics endpoints) but is missing most of the fleet infrastructure:
no tests, no docs, no CLAUDE.md, no session context injection, no CI, no MCPB packaging,
no pre-commit config, no skills.

## Per-severity count

CRITICAL=0, HIGH=3, MEDIUM=10, LOW=6

## Detailed Findings

### HIGH

| Issue | Category | Detail |
|-------|----------|--------|
| No tests at all (tests/ dir missing) | Testing | No unit/integration tests exist |
| No docs/ directory | Required | No docs/ with CONFIGURATION, DEVELOPMENT, TOOLS, TROUBLESHOOTING |
| Dirty working tree | Git | justfile, uv.lock modified; justfile.bak untracked |

### MEDIUM

| Issue | Category | Detail |
|-------|----------|--------|
| No CLAUDE.md | Required Files | Per-repo agent instructions missing |
| No .cursorrules | Session Context | Cursor tool-awareness missing |
| No .claude-plugin/ | Session Context | Claude Code session injection missing |
| No .pre-commit-config.yaml | Required Files | Fleet pre-commit not configured |
| justfile missing mcpb-pack | Required Files | MCPB packaging recipe missing |
| justfile missing gates-green/certify | Required Files | Gate verification recipe missing |
| No skills/ directory | Tool Surface | skill:// resources and SKILL.md missing |
| No `_error_response()` auto-logging | Error Handling | Pattern 3 not implemented |
| No logger in server.py | Error Handling | Logging only configured in __main__.py |
| Dialogic returns missing `data` key | Tool Surface | Returns have `message` but no `data` key |

### LOW

| Issue | Category | Detail |
|-------|----------|--------|
| No CHANGELOG.md | Required Files | No change tracking |
| No .windsurfrules | Session Context | Missing copy of cursorrules |
| No .github/copilot-instructions.md | Session Context | Copilot tool-awareness missing |
| No .opencode/skills/ | Session Context | OpenCode skill missing |
| .gitignore missing `reports/`, `*.mcpb` | Required Files | Build artifacts not excluded |
| Leftover .bak file | Git Hygiene | justfile.bak-20260729_133948 untracked |

### Passes (notable green checks)

| Check | Finding |
|-------|---------|
| FastMCP pin >=3.4.4,<4 | ✓ |
| CORS middleware | ✓ correct origins + unconditional regex |
| HEALTH endpoint | ✓ GET /api/health |
| DIAGNOSTICS endpoint | ✓ GET /api/v1/diagnostics |
| Dual transport | ✓ run_server.py + __main__.py |
| Tool portmanteau | ✓ vfx_apply with 9 operations |
| Docstring Return Format | ✓ present |
| Docstring Examples | ✓ present |
| No Args: blocks | ✓ uses Annotated+Field |
| Port registry | ✓ 11122 registered, no conflict |
| uv.lock committed | ✓ |
| .env.example | ✓ clean, no secrets |
| start.ps1 | ✓ port clearing + readiness poll |
| AGENTS.md | ✓ |
| llms.txt + llms-full.txt | ✓ |
| glama.json | ✓ |
| ruff config | ✓ |
| Annotations on tool | ✓ readonly flag |

### Dockerization Note

No Dockerfile or docker-compose.yml. Consider Dockerizing for one-command
startup without Python/Node on the host. Not a requirement — flagged as INFO.

### ghaudit

ghaudit has never been run on this repo.
